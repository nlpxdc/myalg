package structure.logic.kv.map.common;

//map 是一种方法，接口，不是对象、元组
//是二元组，三元组，四元组，n元组，递归表示嵌套，和其他都不一样
//所以jdk的map是接口，entry也是，是被实现而非继承
//是一种映射 函数结构，强调功能
//用hash，天然适配
//或用tree，需要区间时
class MyMapApp {
    public static void main(String[] args) {
        System.out.println("aa");
    }

    //通过key，找val值，找Object和T
    //hash map
    //tree map
}
